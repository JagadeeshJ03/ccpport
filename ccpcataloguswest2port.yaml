apiVersion: s3.aws.upbound.io/v1beta1
kind: Bucket
metadata:
  name: ccpcataloguswest2port
spec:
  forProvider:
    region: us-west-2
    tags:
      project: CCP
      env: dev
      aws: eks
  providerConfigRef:
    name: newawsconfig
